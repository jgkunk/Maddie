dir()
# Reading AFM text files and print an array of boxed 3D images, 1 for each text file.
require(lattice)
# DEFINE 'ask' function
ask<- function(text) {
cat(text,": ", sep="")
indata<- readLines(con=stdin(), n=1, ok = TRUE)
}
ltxt<- dir(patt='[.]txt')
print(ltxt)
innum= as.numeric(ask("Enter an index number of a file file to start with"))
innum= as.numeric(ask("Enter an index number of a file file to start with"))
innum= as.numeric(ask("Enter an index number of a file file to start with"))
innum= as.numeric(ask("Enter an index number of a file file to start with"))
innum= as.numeric(ask("Enter an index number of a file file to start with"))
innum= as.numeric(ask("Enter an index number of a file file to start with"))
